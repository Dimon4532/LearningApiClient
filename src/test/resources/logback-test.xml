<configuration>
    <!-- Уровень для пакета текущего проекта -->
    <logger name="ru.learning.java" level="DEBUG" />

    <!-- Корневой уровень -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>